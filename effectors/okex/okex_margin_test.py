import unittest


# ensure events that arrive late in fired window are not emitted
# ensure that events occur in temporal coherence
# ensure that 

class EnrichDepthsTest(unittest.TestCase):

    def test_hourly_team_score(self):
        pass

if __name__ == '__main__':
    logging.getLogger().setLevel(logging.INFO)
    unittest.main(argv=['first-arg-is-ignored'], exit=False)